import type { CliFlags } from './types.js';

/** 主流程占位:M2 里接上 detect → 问答 → plan → write。 */
export async function run(_flags: CliFlags): Promise<void> {
  console.log('cc-kickstart: 核心流程尚未接线(M2)。当前可用 --version / --help。');
}
