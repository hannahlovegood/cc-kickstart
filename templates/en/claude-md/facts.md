## Verified technical facts (do not re-learn these the hard way)

Pitfalls already hit, version constraints already verified, counter-intuitive environment quirks — record them here. Each line saves the next session one round of debugging.

- (Format example: `X must stay pinned at vN — behavior changed in vN+1 and upgrading breaks Y`.)
