## Common commands

{{commandsBlock}}
{{#if showCheckTip}}
Consider adding a `"check": "…"` script to package.json that chains typecheck + test + build — one command to know whether the project is healthy, run it before wrapping up.
{{/if}}
